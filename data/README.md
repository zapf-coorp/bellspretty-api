# Este diretório contém os arquivos de banco de dados SQLite para desenvolvimento
# Os arquivos .sqlite são ignorados pelo git (ver .gitignore)

data/
├── development.sqlite  (criado automaticamente)
└── test.sqlite        (criado automaticamente para testes)