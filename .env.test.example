# Test environment variables
NODE_ENV=test
PORT=3001

# Test database (in-memory SQLite)
# Não precisa configurar DB_DATABASE para testes - usa :memory:

# JWT para testes
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=1h

# API configuration
API_PREFIX=api